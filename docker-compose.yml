version: '3.8'

services:
  web:
    build: .
    container_name: biocorpore-web
    restart: unless-stopped
    networks:
      - app-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.biocorpore-rmti.entrypoints=https"
      - "traefik.http.routers.biocorpore-rmti.rule=Host(`biocorpore.rmti.com.br`)"
      - "traefik.http.routers.biocorpore-rmti.tls=true"
      - "traefik.http.services.biocorpore-rmti.loadbalancer.server.port=80"
      - "traefik.docker.network=app-network"

networks:
  app-network:
    external: true